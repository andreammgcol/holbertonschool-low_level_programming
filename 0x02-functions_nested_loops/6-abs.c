#include "holberton.h"
/**
 * _abs - that compute the absolute value.
 * @a: number to be checked
 * Return: a if the number is positive.
 */
int _abs(int a)
{
if (a < 0)
{
a = a * -1;
return (a);
}
else
{
return (a);
}
}
